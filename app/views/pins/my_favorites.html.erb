<h1>My favorite list</h1>

<% if current_user.find_liked_items.size > 0 %>
<ul class="list-group">
  <li class="list-group-item">
    <span class="badge"><%= current_user.find_liked_items.size %></span>
      <b>Restaurants:</b>
       <%= render 'pins/show_pin_details' %>  
    </li>
</ul>

<%= render 'pins/show_pin_details' %>  
<% else %>
	<p> You haven't added any restaurants yet, start now with adding	 some restaurants to your favorite list here. </p>
	<%= link_to pins_path, class: "btn btn-danger btn-block" do %>
	        <span class="glyphicon glyphicon-chevron-right"></span>
	        Add restaurants to favorite list
	        <% end %></p>
<% end %>

<%= link_to pins_path, class: "btn btn-default btn-block" do %>
	<span class="glyphicon glyphicon-chevron-right"></span>
	View all restaurants
	<% end %></p>


