<div class="panel panel-default">
  <div class="panel-heading">
     <div class="panel-title">
      <h1>Looking for the best restaurants in town? </h1>
      </div>
       <div class="panel-heading">
       Save your favorite restaurant to your favorite list, view your friend´s favorite list and receive personal suggestions to discover your new favorite restaurant. Log in and start right now!
    </div>
<div class="panel-body">
        <%= link_to "Log in", user_omniauth_authorize_path(:facebook), class: "btn btn-danger btn-lg" %><br/><br/>
      <b>Note:</b> Dinder <u> will never post messages on your Facebook wall</u>.
      </div>
      </div>
</div>
</div>

<div class="panel-body">
    <%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
      <%= devise_error_messages! %>

      <div class="form-group">
        <%= f.label :email, "Email address" %>
        <%= f.email_field :email, autofocus: true, class: "form-control", :autofocus => true %>
      </div>

      <div class="form-group">
        <%= f.label :password, "Password" %>
        <%= f.password_field :password, class: "form-control" %>
      </div>

      <div class="form-group">
        <%= f.submit "Log in", class: "btn btn-info" %>
      </div>
    <% end %>
  </div>

